# CLAUDE.md

이 파일은 Claude Code가 이 저장소에서 작업할 때 참고하는 프로젝트 컨텍스트다.

## 프로젝트

**ARK** — 마인크래프트 **NeoForge 1.21.x** 기반 RPG 모드.
- **데이터팩 주도(data-driven)** 설계: 모드는 시스템·코덱·엔진을 제공, 콘텐츠는 데이터팩 JSON으로 정의.
- 모드 ID: `ark` (소문자) · 데이터팩 경로 `data/ark/...`
- 핵심 비전: **전투·스킬 중심 액션 RPG + "살아있는 세계"**.

## 현재 상태

**게임 디자인 단계 (코드 구현 전).** 아직 Gradle 프로젝트·소스 없음. 설계만 진행 중.

## 가장 중요한 참고 문서

> 작업 전 **반드시 먼저 읽을 것**: [`docs/design/2026-06-01-ARK-게임디자인-메모.md`](docs/design/2026-06-01-ARK-게임디자인-메모.md)
>
> 모든 게임 디자인 결정(스탯 체계, 성장, 마나, 마수화, 클래스 트리, 무기, 스킬, 결정 로그, 영문 이름/ID, 열린 숙제)이 여기에 기록돼 있다. 설계가 바뀌면 이 메모를 갱신한다.

## 설계 핵심 (요약 — 상세는 메모 참고)

- **스탯**: 기본 3종(힘·민첩·지능) → 하위 6종(공격/인내·신속/숙련·마력/제압)에 레벨업 포인트 투자(정수). 별도 특수 스탯 **침식**(세계관으로 상승, 마수화 — 추후 설계). 마나는 자원(전체 지능 비례).
- **스탯 엔진**: 하이브리드 접근 — 데이터 주도 스탯, 파생 효과는 가능한 바닐라/커스텀 **Attribute 모디파이어**로 적용. 저장은 **Data Attachment**. 첫 구현 슬라이스는 메모 §3 참고.
- **클래스**: 삼각 방사형 트리, 기본 3클래스(전사·도적·마법사)×3갈래=9빌드 + 공개 하이브리드 3변(궁수·마검사·주술 검객). 마수는 숨겨둔 특수 레이어.
- **스킬**: 데이터팩 정의, 마나+쿨다운, 즉발 5키 + Alt 라디얼 휠, 공격력/마법력 가중치 스케일, 효과 부품 조합.

## 기술 / 환경

- **JDK 21** (Temurin 21.0.9 확인됨). NeoForge 1.21.x는 Java 21 필요.
- 프로젝트 골격은 아직 없음 — 구현 시작 시 NeoForge MDK로 스캐폴딩 예정.
- 리소스 ID는 소문자/숫자/`_`만 (마인크래프트 규칙).

## 협업 방식 (중요)

- 사용자와 **한국어**로 진행. 사용자가 **한 번에 하나씩** 결정하길 원함 — 한꺼번에 여러 질문/제안을 쏟아내지 말 것.
- 사용자가 명시적으로 "다음" 신호를 줄 때만 다음 단계로 넘어간다. 멋대로 결정·확장하지 말 것.
- 설계 결정이 나오면 그때그때 위 메모에 기록한다.

## 작업 분담 (사용자 워크플로)

- **PC(집, 이 워크스페이스 + Claude Code) = 구현.** 코드, Gradle, 스캐폴딩, 시스템 구현.
- **외부(폰, GitHub 웹 직접 편집) = 구상.** 게임 디자인 메모 직접 갱신, 세계관·스킬·세력 등 큰 틀 살붙이기.
- 따라서 **세션 시작 시 `git fetch && git pull` 필수** — 사용자가 깃허브에서 메모를 크게 갱신해뒀을 수 있음. 처음 보는 섹션·내용이 늘어나 있어도 정상이다(외부에서 추가한 것).

## Git

- 원격: `github.com/attitude433/ARK` (`main`).
- 설계 변경 시 메모/README 갱신 후 커밋·푸시해 워크스페이스와 원격을 최신 상태로 유지.
- 자동 생성되는 `claude/*` 리뷰 브랜치가 보일 수 있음 — *메모를 통째로 축약·삭제할 수 있어 위험*. 안의 유용한 조각(예: `.gitignore` 패턴)만 발췌해 메인에 보강하고, 브랜치 자체는 머지하지 말 것.
